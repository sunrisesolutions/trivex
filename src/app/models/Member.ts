import { NgbDate } from "@ng-bootstrap/ng-bootstrap";

export class Member {
    firstName: string;
    lastName: string;
    dateOfBirth: NgbDate;
    phone: string;
    company: string;
    role: string;
    email: string;
    password: string;
    status: string;
}